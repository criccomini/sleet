# The full budget product for specs/mirror-race.fizz: liveness needs
# the full graph, which does not fit in memory at these budgets, so
# they are spot-checked by random simulation instead (safety
# assertions hold per trace; exists assertions are skipped).
# Run: fizz -x --max_runs 1 --seed <n> \
#        --preinit-hook-file specs/mirror-race-sim.cfg specs/mirror-race.fizz
WRITES = 2
SWEEPS = 2
