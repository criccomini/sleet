# The full budget product for specs/mirror.fizz: too large for
# exhaustive checking, spot-checked by random simulation instead
# (safety assertions hold per trace; exists assertions are skipped).
# Run: fizz -x --max_runs 1 --seed <n> \
#        --preinit-hook-file specs/mirror-sim.cfg specs/mirror.fizz
WRITES = 2
EXPIRIES = 1
PRUNES = 2
